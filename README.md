# 🚀 Terraform K3s E-commerce Demonstration

This repository contains a Terraform configuration designed to provision infrastructure, which could host a simple K3s cluster and e-commerce stack. This demonstration is focused on illustrating the core **Terraform workflow** (`init`, `plan`, `apply`) using a simple setup script.

## 📋 Prerequisites

UBUNTU 22 (preferably)

## 🛠️ Getting Started : just run the following commands as SUDO

git clone https://github.com/Benguitoun-Abdelbasset/terratest.git

cd terratest/k3s-ecom

chmod +x install_terraform.sh

./install_terraform.sh

terraform init

terraform plan

terraform apply
